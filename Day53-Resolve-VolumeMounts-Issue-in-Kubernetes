             Question
      We encountered an issue with our Nginx and PHP-FPM setup on the Kubernetes cluster this morning, which halted its functionality. Investigate and rectify the issue:

The pod name is nginx-phpfpm and configmap name is nginx-config. Identify and fix the problem.

Once resolved, copy /home/thor/index.php file from the jump host to the nginx-container within the nginx document root. After this, you should be able to access the website using Website button on the top bar.

Note: The kubectl utility on jump_host is configured to operate with the Kubernetes cluster.

               Solution
 1- Create the configmap
      vi nginx-config.yaml
       paste the correcte yaml file
 2- Apply the configmap
     kubectl apply -f nginx-config.yaml
 3- Copy the index.php into container
     kubectl cp /home/thor/index.php nginx-phpfpm:/var/www/html/index.php -c nginx-container
      • -c nginx-container: ensures the file goes into the Nginx container, not PHP-FPM.
      • /var/www/html: is the Nginx document root.
 4- Verify Inside the Nginx Container
     kubectl exec -it nginx-phpfpm -c nginx-container -- ls -l /var/www/html/
      .You should see index.php
 5- Verify the website using the button on the top bar
